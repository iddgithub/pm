# Demo验收清单

演示前至少检查：

- 首页、列表页、详情页、弹窗页是否都能进入
- 关键主流程是否能从头走通
- 关键状态是否都有展示，不只展示理想状态
- 文案是否统一，按钮命名是否符合业务语义
- 表单是否有默认值、校验和错误提示
- 表格、卡片、统计区是否存在明显假数据穿帮
- 菜单、面包屑、返回路径是否一致
- 空态、加载态、异常态是否至少覆盖最关键页面
- 演示用 mock 数据是否和当前业务规则一致
- 需要口头解释的地方，是否已提前准备备注
