# 02_tools

这里是 `pm_agent` 的能力层之二，专门放模板、清单和工作流。

如果说 `01_agents/` 负责“AI 用什么身份做事”，那么 `02_tools/` 负责“做事时套用什么结构”。

## 当前结构

- `模板/`：适合复制后快速开工的输入模板或文档骨架
- `清单/`：适合评审、自查、验收的 checklist
- `工作流/`：适合把一类任务跑通的步骤说明

## 使用原则

1. 工具文档要能直接复用，避免写成空泛理论。
2. 优先服务你当前最高频的 PM 工作，而不是一次性铺满所有场景。
3. 工具更新后，同步更新本目录索引。
